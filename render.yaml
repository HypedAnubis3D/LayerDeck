services:
  - type: web
    name: layerdeck-api
    runtime: node
    rootDir: artifacts/api-server
    buildCommand: npm install && node build.mjs
    startCommand: node dist/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000